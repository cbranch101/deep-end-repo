// review the results in the terminal and find the line with multiply-numbers

// Note its color and icon

// click on the tab for multiply-numbers.js
// change the a * b on line 2 to a - b
// save the change

// attempt the first trial

// Note the change to the multiply-numbers line and the information beneath
// Undo the change(command z) and save again(command s)

// Make the required changes every file in neophyte so that
// every row in the terminal output has a green checkmark
